{
  "name": "processing",
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "test": "bun ./scripts/run-tests.ts",
    "lint": "oxlint --fix .",
    "format": "oxfmt --write -c ../app/oxfmt.config.mjs .",
    "check": "bun run --parallel typecheck lint format test",
    "check-lint": "oxlint .",
    "check-format": "oxfmt --check -c ../app/oxfmt.config.mjs .",
    "typecheck": "bun ./scripts/run-typecheck-lua.ts"
  },
  "dependencies": {
    "@date-fns/tz": "^1.4.1",
    "@turf/turf": "^7.3.2",
    "date-fns": "^4.1.0",
    "geojson": "^0.5.0",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "oxfmt": "^0.52.0",
    "oxlint": "^1.66.0"
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  }
}
