/** Ranked list size when extent is `all` (biggest |absoluteChange| first). */
export const QA_AREA_LIST_LIMIT_ALL = 50

/** Safety valve when extent is `view` (filters already bound the set). */
export const QA_AREA_LIST_LIMIT_VIEW = 500
