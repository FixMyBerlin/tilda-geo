import { modeIdentity } from '@/components/regionen/pageRegionSlug/modes/modeIdentity'

/** Halo around a selected Prüfeintrag; the status pin/line/fill stays unchanged. */
export const REVIEW_ENTRY_SELECT_HALO = modeIdentity.reviewLists.accent

/** TerraDraw / move-armed geometry. Named in the detail help text. */
export const REVIEW_ENTRY_MOVE_COLOR = '#ea580c'
export const REVIEW_ENTRY_MOVE_COLOR_LABEL = 'orange'
