# Needed when running `vite build && bun run .output/server/index.mjs` (Vite-only)
# Ignored by .dockerignore; build uses ARG VITE_APP_* in app.Dockerfile
VITE_APP_ORIGIN=http://127.0.0.1:4000
VITE_APP_ENV=staging
